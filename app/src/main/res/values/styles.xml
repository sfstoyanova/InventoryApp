<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Editor Activity Button Theme -->
    <style name="EditorButtonStyle">
        <!-- Customize your theme here. -->
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
        <item name="android:background">@color/colorAccent</item>
        <item name="android:textColor">@color/colorButtonText</item>
    </style>

    <!-- Editor Activity Category Theme -->
    <style name="CategoryStyle">
        <!-- Customize your theme here. -->
        <item name="android:textAppearance">?android:textAppearanceSmall</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginLeft">@dimen/layout_margin_padding_16</item>
        <item name="android:layout_marginRight">@dimen/layout_margin_padding_8</item>
    </style>

    <!-- Editor Activity Edit fields Theme -->
    <style name="EditFieldsStyle">
        <!-- Customize your theme here. -->
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
        <item name="android:textColor">@color/colorPrimaryText</item>
        <item name="android:textColorHint">@color/colorSecondaryText</item>
    </style>

</resources>
